# File verification

An operator may submit an eligible file for verification. A submitted file cannot be submitted
again until a retry is explicitly available.

## Retry

A file whose verification query timed out may be retried. The retry restarts its polling counter.
