# Context Loom workflow Skill

This is the user-facing entry point for the Context Loom runtime. It explains when to use the
framework; the deterministic implementation lives in `src/context_loom/`.

