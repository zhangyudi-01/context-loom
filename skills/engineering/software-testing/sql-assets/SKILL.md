---
name: testing-sql-assets
description: Plan, validate, reuse, and converge read-only SQL assets used by software-testing workflows without inventing database facts.
---

# Testing SQL assets adapter

SQL is a domain asset, not a Context Loom core concern. Plan it once, reuse identical query
semantics, validate parameters and database compatibility, and keep SQL references traceable to
the test data closure that authorizes them.

